---
"hex-app": patch
---

Fix the Chip menu-bar pill not filling the menu-bar height (it was hugging the glyph's intrinsic size); the pill now fills the bar like the system mic pill and ChipSpec.chipVMargin controls its height
