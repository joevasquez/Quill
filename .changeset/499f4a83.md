---
"hex-app": patch
---

Chip menu-bar mode: fix the feather rendering dark/invisible and pulsing to transparent on some displays (now a solid white feather on a consistent dark chip), and size the pill to fill the menu bar so it matches the system mic pill height
