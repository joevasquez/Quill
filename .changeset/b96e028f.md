---
"hex-app": minor
---

MCP client: connect any Model Context Protocol server (Settings → Agent → Tools) and its tools become voice-invocable — Hermes lists them to the planner, shows the call on the confirmation card, and executes over Streamable HTTP with keychain-stored tokens
