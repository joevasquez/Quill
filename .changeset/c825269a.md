---
"hex-app": minor
---

Chained Action steps: say "look up Joe's email in Dex and then draft him a birthday email in Gmail" — a step can now consume an earlier step's result. The agent runs the lookup first, extracts what the next step needs (the email address) via a small resolve pass, and fills it in before drafting
