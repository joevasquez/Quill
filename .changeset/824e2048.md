---
"hex-app": patch
---

Detail lookups now chain automatically: asking for a contact's email runs the search, then fetches the full record with the found id — search tools only return summaries, so single-step lookups couldn't answer detail questions
