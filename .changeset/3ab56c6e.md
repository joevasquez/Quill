---
"hex-app": patch
---

MCP tool results in the Action confirmation panel are now formatted for readability — a contact lookup shows a clean name + title + location instead of a raw one-line JSON blob (works for any MCP server, drops ids/avatars/counts)
