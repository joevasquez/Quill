---
"hex-app": minor
---

Type a command: run agent actions from the keyboard — Type a Command in the Mac menu bar and a keyboard button in the iPhone capture cluster, using the same pipeline as voice (routines, memory, connections)
