---
"hex-app": patch
---

Surface why an action failed in the confirmation panel (instead of a silent auto-dismiss), and make MCP authentication failures explain that the server needs a token / that OAuth servers aren't supported yet
