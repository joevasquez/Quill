---
"hex-app": minor
---

MCP servers that require a browser sign-in (OAuth) now work — Quill runs the full MCP auth flow (discovery, dynamic client registration, PKCE browser login, token refresh), so servers like Dex, Notion, and Linear can be connected, not just static-token servers
