---
"hex-app": patch
---

MCP servers in Settings → Agent → Tools can now be edited (name, URL, token), not just added and deleted — a pencil button on each row opens an edit sheet; the stored token is preserved unless you replace or remove it
