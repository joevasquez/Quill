---
"hex-app": patch
---

Chained Action commands now show what happened: the confirmation panel lists each step's outcome — the contact that was looked up (name, title, email) and the email that was drafted (To, Subject, Body) — instead of a bare "2 created" that auto-dismissed before you could read it
