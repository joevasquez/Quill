---
"hex-app": minor
---

Agent memory extraction now runs on-device via Apple's Foundation Models when available (macOS 26+, Apple Intelligence) — learning from dictations is free and the transcript never leaves the Mac; falls back to the cloud provider otherwise
