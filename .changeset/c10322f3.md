---
"hex-app": patch
---

Act command bar: render @ tags as small inline pills in the typed text (macOS), close the suggestion menu once a destination is picked, position the menu below the field instead of over it, and loosen vertical spacing on the Mac Home pane.
