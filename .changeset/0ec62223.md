---
"hex-app": minor
---

macOS: add an Appearance control (Auto/Light/Dark) in Settings → General, matching iOS — Auto follows your Mac's system setting, and the override applies across every window (Settings, Notes, the HUD/orb).
