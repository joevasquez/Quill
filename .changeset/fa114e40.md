---
"hex-app": patch
---

Stop the macOS keychain password prompts on rebuild: secrets now use the Data Protection keychain (no prompts) with a transparent one-time migration from the legacy keychain, so existing API keys and tokens are preserved
