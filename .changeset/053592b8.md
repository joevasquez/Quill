---
"hex-app": patch
---

Fix an inline edit pasting the raw AI prompt into your document when the edited text happened to begin like a refusal ('I can't...', 'I'm sorry...'). Auto mode now recognizes 'convert this into bullets' and similar phrasings as edits. Auto also records what it decided, locally, so misroutes can be measured.
