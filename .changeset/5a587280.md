---
"hex-app": patch
---

Make MCP tool calls reliable across servers: the planner now always gets each tool's argument names, types, and required flags (parsed from its schema) instead of dropping large schemas, so tools like Dex's contact search get their required 'query' argument
